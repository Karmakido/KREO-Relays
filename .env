# Admin UI defaults (safe placeholders)
# Copy/adjust as needed; secrets (tokens) should be injected at runtime or stored securely.

ADMIN_HOST=0.0.0.0
ADMIN_PORT=4000
ADMIN_TOKEN=

# Git auto-push (optional)
GIT_AUTO_PUSH=0
GIT_REMOTE=origin
GIT_BRANCH=main
GIT_COMMIT_MSG=Update relays.json via admin UI
GITHUB_TOKEN=
GITHUB_RELAYS_URL=

# Docker helpers
DOCKER_IMAGE=kreo-relays-admin
RELAYS_JSON=relays.json
